{
  "name": "facerecognitionbrain",
  "homepage": "https://souhani.github.io/smartBrainAppDockerized",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.27.2",
    "bootstrap": "^5.3.2",
    "clarifai": "^2.9.1",
    "gh-pages": "^6.1.1",
    "react": "^16.8.0",
    "react-dom": "^16.8.0",
    "react-particle-backgrounds": "^1.0.4",
    "react-scripts": "^4.0.2",
    "reactstrap": "^9.2.0",
    "tachyons": "^4.12.0",
    "workbox-webpack-plugin": "^6.0.0-alpha.3"
  },
  "overrides": {
    "clarifai": {
      "axios": "$axios"
    }
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "web-vitals": "^3.1.1"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  }
}
